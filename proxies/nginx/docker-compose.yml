version: "3.8"

services:
  cors-proxy:
    image: nginx:1.27-alpine
    container_name: ngsild-cors-nginx
    ports:
      - "1027:1027"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
